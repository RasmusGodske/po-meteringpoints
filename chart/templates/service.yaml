apiVersion: v1
kind: Service
metadata:
  name: {{ include "ett-meteringpoints.fullname" . }}
  labels:
    {{- include "ett-meteringpoints.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ett-meteringpoints.selectorLabels" . | nindent 4 }}
    component: {{ .Values.service.deployment }}
